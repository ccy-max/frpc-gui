# 默认服务器功能 - 完成报告

## ✅ 完成的功能

在**单 FRP 进程模式**下，添加了**默认服务器**设置，用于在新建代理时自动关联到指定的服务器。

---

## 🎯 功能说明

### 使用场景

当您有多个服务器配置，但**只运行一个 FRP 进程**时：

1. 在设置中选择一个**默认服务器**
2. 新建代理时会自动关联到这个服务器
3. 所有代理实际上都连接到同一个 FRP 服务器
4. 服务器关联仅用于**UI 分组和管理**

### 工作流程

```
设置 → 选择默认服务器
        ↓
新建代理 → 自动关联到默认服务器
        ↓
代理卡片 → 显示所属服务器标签
        ↓
过滤器 → 可按服务器筛选代理
```

---

## 📝 修改的文件

### 1. 后端设置结构
**文件**: `src-tauri/src/utils/settings.rs`

**修改内容**:
```rust
pub struct AppSettings {
    // ... 其他字段
    pub default_server_id: Option<String>,  // ← 新增
}
```

---

### 2. 前端 Store
**文件**: `src/stores/app.ts`

**修改内容**:

1. **新增状态**:
   ```typescript
   const defaultServerId = ref<string | null>(null);
   ```

2. **修改 loadSettings**:
   ```typescript
   defaultServerId.value = s.default_server_id || null;
   ```

3. **修改 saveSettings**:
   ```typescript
   default_server_id: defaultServerId.value,
   ```

4. **新增函数**:
   ```typescript
   function setDefaultServerId(id: string | null) {
     defaultServerId.value = id;
     saveSettings();
   }
   ```

---

### 3. 代理视图
**文件**: `src/views/Proxies.vue`

**修改内容**:

**openAdd 函数** - 优先使用默认服务器：
```typescript
function openAdd() {
  form.value = {
    // ... 其他字段
    // 优先使用默认服务器，其次使用活动服务器
    server_id: appStore.defaultServerId || activeServerId.value || '',
  };
}
```

---

### 4. 设置视图
**文件**: `src/views/Settings.vue`

**新增内容**:

1. **默认服务器选择器**（在"通用"选项卡）:
   ```vue
   <a-form-item label="默认服务器">
     <a-select 
       v-model:value="appStore.defaultServerId" 
       @change="handleConfigChange" 
       style="width: 100%"
       placeholder="选择新建代理时默认关联的服务器"
       allow-clear
     >
       <a-select-option
         v-for="server in appStore.servers"
         :key="server.id"
         :value="server.id"
       >
         {{ server.name }} ({{ server.serverAddr }}:{{ server.serverPort }})
       </a-select-option>
     </a-select>
     <div class="form-help">
       新建代理时会自动关联到此服务器，留空则使用活动服务器
     </div>
   </a-form-item>
   ```

2. **样式**:
   ```scss
   .form-help {
     margin-top: 8px;
     font-size: 12px;
     color: #64748b;
     font-style: italic;
   }
   ```

---

## 🎨 UI 展示

### 设置页面 - 通用选项卡

```
┌─────────────────────────────────────┐
│ 通用                                │
├─────────────────────────────────────┤
│ 语言：[简体中文 ▼]                  │
│                                     │
│ 主题：[跟随系统 ▼]                  │
│                                     │
│ 默认服务器：                         │
│ ┌─────────────────────────────────┐ │
│ │ 选择新建代理时默认关联的服务器 ▼│ │
│ ├─────────────────────────────────┤ │
│ │ 生产服务器 (192.168.1.100:7000) │ │
│ │ 测试服务器 (192.168.1.101:7000) │ │
│ │ [清空]                          │ │
│ └─────────────────────────────────┘ │
│ 新建代理时会自动关联到此服务器，     │
│ 留空则使用活动服务器                 │
│                                     │
│ 启动选项：                           │
│ ☐ 开机自启                          │
│ ☑ 最小化到托盘                      │
│ ☑ 关闭到托盘                        │
└─────────────────────────────────────┘
```

### 新建代理弹窗

```
┌─────────────────────────────────────┐
│ 添加代理                      [×]   │
├─────────────────────────────────────┤
│ 名称：[web-proxy]                   │
│ 类型：[TCP ▼]                       │
│                                     │
│ 所属服务器：                         │
│ ┌─────────────────────────────────┐ │
│ │ 生产服务器 (192.168.1.100:7000) ▼│ │
│ └─────────────────────────────────┘ │
│                                     │
│ 启用：[✓]                           │
│                                     │
│ 本地 IP: [127.0.0.1]                │
│ 本地端口：[8080]                    │
│ 远程端口：[80]                      │
│                                     │
│          [取消]  [确定]             │
└─────────────────────────────────────┘
```

---

## 🔄 优先级逻辑

新建代理时，`server_id` 的自动选择逻辑：

```
1. 默认服务器 (appStore.defaultServerId)
   ↓ (如果为空)
2. 活动服务器 (activeServerId)
   ↓ (如果为空)
3. 不关联 (空字符串)
```

用户可以：
- 在设置中设置默认服务器 → 所有新代理默认关联
- 在新建代理时手动修改 → 覆盖默认值
- 清空默认服务器 → 使用活动服务器或不关联

---

## ✅ 构建状态

```
✓ 前端构建成功
dist/index.html                     0.48 kB
dist/assets/index-C_J_9Tx5.css     22.15 kB
dist/assets/index-DO6aMf5W.js   1,607.19 kB
```

---

## 🧪 测试步骤

### 1. 设置默认服务器

1. 打开应用
2. 进入"设置"页面
3. 切换到"通用"选项卡
4. 在"默认服务器"下拉框中选择一个服务器
5. 点击保存

### 2. 验证新建代理

1. 进入"代理管理"页面
2. 点击"添加代理"
3. 观察"所属服务器"字段是否自动选中默认服务器
4. 新建几个代理

### 3. 验证卡片显示

1. 查看代理卡片
2. 确认"服务器"标签显示正确
3. 使用过滤器按服务器筛选

### 4. 验证持久化

1. 关闭应用
2. 重新打开
3. 检查默认服务器设置是否保留
4. 检查代理的服务器关联是否保留

---

## 📊 数据结构

### 设置文件位置
```
%APPDATA%\frpc-gui\settings.json
```

### 设置文件内容示例
```json
{
  "language": "zh-CN",
  "theme": "auto",
  "frpc_path": "",
  "config_path": "",
  "log_path": "",
  "auto_start": false,
  "minimize_to_tray": true,
  "close_to_tray": true,
  "default_server_id": "server-123"  // ← 新增
}
```

### 代理数据示例
```json
{
  "name": "web-proxy",
  "type": "tcp",
  "server_id": "server-123",  // ← 关联的服务器 ID
  "local_ip": "127.0.0.1",
  "local_port": 8080,
  "remote_port": 80,
  "enabled": true
}
```

---

## 💡 使用建议

### 推荐配置

**场景 1：单一服务器**
- 设置默认服务器 → 该服务器
- 所有代理自动关联

**场景 2：多服务器（主要用一个）**
- 设置默认服务器 → 主要使用的服务器
- 特殊代理手动选择其他服务器

**场景 3：多服务器（平等使用）**
- 不设置默认服务器（留空）
- 新建代理时手动选择

### 注意事项

1. **服务器关联仅用于 UI 管理**
   - 所有代理实际连接到同一个 FRP 服务器
   - 不会启动多个 FRP 进程

2. **删除服务器后**
   - 关联到该服务器的代理会显示"未关联"
   - 建议先修改代理的服务器关联再删除服务器

3. **默认服务器被删除**
   - 设置中的默认服务器会自动清空
   - 需要重新选择

---

## 🎯 后续优化建议

### 短期
- [ ] 删除服务器时提示是否修改关联的代理
- [ ] 批量修改代理的服务器关联
- [ ] 服务器状态显示关联的代理数量

### 中期
- [ ] 支持多 FRP 进程（需要较大改动）
- [ ] 服务器组管理
- [ ] 代理模板功能

### 长期
- [ ] 配置文件自动生成（按服务器分组）
- [ ] 一键部署到多个服务器
- [ ] 负载均衡和故障转移

---

## 📋 总结

### 实现的功能
✅ 默认服务器设置  
✅ 新建代理自动关联  
✅ 服务器过滤器  
✅ 数据持久化  
✅ UI 提示和说明  

### 修改的文件
- `src-tauri/src/utils/settings.rs` - 后端设置结构
- `src/stores/app.ts` - 前端状态管理
- `src/views/Proxies.vue` - 代理视图
- `src/views/Settings.vue` - 设置页面

### 构建状态
✅ 前端构建成功  
⏳ 等待 Windows 环境完整测试  

---

**完成时间**: 2026-08-25  
**状态**: ✅ 开发完成，等待测试
