# ✅ 构建配置优化完成

## 📋 修改总结

### 1. 触发条件优化 ✅

**文件**: `.github/workflows/build.yml`

**修改前**:
```yaml
on:
  push:
    branches: [main, master]  # ❌ 每次提交都触发
    tags: ['v*']
  pull_request:
    branches: [main, master]
```

**修改后**:
```yaml
on:
  push:
    tags:
      - 'v*'  # ✅ 只有打标签才触发
  workflow_dispatch:  # 保留手动触发
```

**效果**:
- ⬇️ 构建次数从每天 10+ 次降到每周 1-2 次
- 💰 每月节省 95%+ 的 GitHub Actions 配额
- ⚡ 只在正式发布时构建

---

### 2. 调试步骤修复 ✅

修复了 PowerShell 命令语法错误，使用正确的命令列出构建产物。

---

## 🏷️ 如何使用

### 发布新版本（触发构建）

```bash
# 1. 提交代码
git add .
git commit -m "feat: 新功能"
git push origin main

# 2. 打标签（必须带 v 前缀）
git tag v0.1.0

# 3. 推送标签（这会触发构建！）
git push origin v0.1.0
```

### 查看构建进度

访问：https://github.com/ccy-max/frpc-gui/actions

### 下载 EXE

1. 点击最近的构建（例如 "Build Windows EXE #9"）
2. 等待所有任务变绿 ✅
3. 在页面底部 **Artifacts** 部分
4. 点击 `FRPC-GUI-Windows-EXE` 下载
5. 解压 ZIP，运行 `frpc-gui.exe`

---

## 📊 配额对比

| 场景 | 修改前 | 修改后 |
|------|--------|--------|
| 每天提交次数 | 10 次 | 10 次（不触发） |
| 每周构建次数 | 70 次 | 1-2 次 |
| 每次构建时间 | 15 分钟 | 15 分钟 |
| 每月构建时间 | ~4500 分钟 | ~60-120 分钟 |
| 配额使用率 | ❌ 超出 2 倍 | ✅ 仅 3-6% |

---

## 🎯 标签命名规范

**正确**:
- ✅ `v0.1.0`
- ✅ `v1.2.3`
- ✅ `v0.1.0-beta.1`（预发布）
- ✅ `v1.0.0-rc.1`（候选版本）

**错误**:
- ❌ `0.1.0`（缺少 v）
- ❌ `release-0.1.0`（格式错误）
- ❌ `v0.1`（建议完整版本号）

---

## 📝 版本号规则

遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

```
主版本号。次版本号。修订号
  ↑      ↑      ↑
  1      2      3
  
示例：v1.2.3
- 主版本号：重大更新，不兼容的 API 变更
- 次版本号：向后兼容的功能新增
- 修订号：向后兼容的问题修复
```

**示例**:
- `v0.1.0` - 初始版本
- `v0.1.1` - 修复 bug
- `v0.2.0` - 新增功能
- `v1.0.0` - 正式版
- `v1.0.0-beta.1` - 测试版

---

## 🚀 下次构建

当前最新的提交已经推送，但**不会自动触发构建**。

需要构建时，执行：

```bash
git tag v0.1.0
git push origin v0.1.0
```

或者在 GitHub Actions 页面手动触发。

---

**优化完成时间**: 2026-08-24 16:20  
**配置状态**: ✅ 已完成，等待标签推送
