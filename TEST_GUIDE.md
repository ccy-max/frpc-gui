# 测试指南

## 编译步骤

### Windows (推荐)

1. **安装 Rust** (如果尚未安装):
   ```powershell
   winget install Rustlang.Rustup
   ```

2. **安装依赖**:
   ```powershell
   cd frpc-gui
   npm install
   ```

3. **开发模式运行**:
   ```powershell
   npm run tauri dev
   ```

4. **构建发布版本**:
   ```powershell
   npm run tauri build
   ```

### Linux

需要安装 Rust 和相关依赖：
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
```

## 功能测试清单

### 测试 1: 代理重复问题修复

**步骤**:
1. 启动应用
2. 进入"代理管理"页面
3. 点击"添加代理"
4. 填写表单：
   - 名称：test-proxy
   - 类型：TCP
   - 本地 IP: 127.0.0.1
   - 本地端口：8080
   - 远程端口：8080
5. 点击保存
6. **验证**: 代理列表中应该只有 1 条记录，不是 2 条

**预期结果**: ✅ 只显示 1 个代理

---

### 测试 2: 服务器数据持久化

**步骤**:
1. 启动应用
2. 进入"服务器管理"页面
3. 点击"添加服务器"
4. 填写表单：
   - 名称：test-server
   - 服务器地址：192.168.1.100
   - 端口：7000
   - 令牌：test123
5. 点击保存
6. 完全关闭应用
7. 重新启动应用
8. 进入"服务器管理"页面

**预期结果**: ✅ test-server 依然存在

**验证数据文件**:
- Windows: 检查 `%APPDATA%\frpc-gui\frpc-gui-data.json`
- 文件内容应包含刚才添加的服务器

---

### 测试 3: 代理数据持久化

**步骤**:
1. 启动应用
2. 进入"代理管理"页面
3. 点击"添加代理"
4. 创建多个不同类型的代理
5. 完全关闭应用
6. 重新启动应用
7. 进入"代理管理"页面

**预期结果**: ✅ 所有代理依然存在，没有重复

---

### 测试 4: 修改和删除持久化

**步骤**:
1. 启动应用
2. 编辑已有的服务器或代理
3. 保存修改
4. 删除一个服务器或代理
5. 完全关闭应用
6. 重新启动应用

**预期结果**: 
- ✅ 修改的内容已保存
- ✅ 删除的数据不再出现

---

### 测试 5: 重置配置

**步骤**:
1. 启动应用
2. 进入"设置"或"关于"页面
3. 执行"重置所有配置"
4. 完全关闭应用
5. 重新启动应用

**预期结果**: ✅ 所有服务器和代理数据被清空

---

## 数据文件验证

### 查看持久化数据文件

**Windows PowerShell**:
```powershell
Get-Content "$env:APPDATA\frpc-gui\frpc-gui-data.json" | ConvertFrom-Json | ConvertTo-Json -Depth 10
```

**Linux/Mac**:
```bash
cat ~/.config/frpc-gui/frpc-gui-data.json | jq .
```

### 手动编辑测试

1. 关闭应用
2. 编辑 `frpc-gui-data.json`
3. 添加或修改数据
4. 启动应用
5. 验证数据是否加载

---

## 常见问题排查

### 问题 1: 数据没有保存

**检查**:
1. 查看控制台是否有错误信息
2. 检查数据文件是否有写权限
3. 确认 `savePersistentData()` 被调用

### 问题 2: 数据没有加载

**检查**:
1. 查看控制台是否有错误信息
2. 确认数据文件路径正确
3. 确认 JSON 格式正确

### 问题 3: 代理仍然重复

**检查**:
1. 确认前端代码已更新
2. 清除浏览器缓存（如果是 Web 视图）
3. 完全重启应用

---

## 性能测试

### 大量数据测试

1. 添加 50+ 个服务器
2. 添加 100+ 个代理
3. 关闭并重启应用
4. 测量加载时间

**预期**: 加载时间 < 2 秒

---

## 回归测试

确保以下功能依然正常工作：

- [ ] FRP 进程启动/停止
- [ ] 配置文件保存/加载
- [ ] 日志查看
- [ ] 版本管理
- [ ] 设置持久化
- [ ] 热重载配置

---

## 提交前检查

- [ ] 所有测试通过
- [ ] 没有控制台错误
- [ ] 数据文件格式正确
- [ ] 代码已格式化
- [ ] 更新文档
