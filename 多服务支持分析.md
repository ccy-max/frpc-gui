# 多 FRP 服务支持分析

## 当前架构分析

### 现状：❌ 不支持同时运行多个 FRP 服务

**原因**：

1. **单个进程管理器**：
   ```rust
   pub struct AppState {
       pub process_manager: Mutex<Option<FrpProcessManager>>,  // ← 只有一个
       // ...
   }
   ```

2. **单一配置**：
   - 只有一个 `config_manager`
   - 只生成一个 `frpc.toml` 配置文件
   - 所有代理都在同一个配置中

3. **启动逻辑**：
   ```rust
   #[tauri::command]
   pub async fn start_frp(config: FrpConfig, state: State<'_, AppState>) -> Result<bool, String> {
       let mut pm_guard = state.process_manager.lock().await;
       // ← 只能启动一个进程
   }
   ```

## 多服务支持的两种方案

### 方案 A：多配置文件 + 多进程（推荐）⭐

**架构**：
```
AppState
├── process_managers: Mutex<HashMap<String, FrpProcessManager>>  // 多个进程
└── config_managers: Mutex<HashMap<String, ConfigManager>>       // 多个配置
```

**实现**：
- 每个服务器配置对应一个独立的 FRP 进程
- 每个进程使用独立的配置文件（如 `server-1.toml`, `server-2.toml`）
- 每个进程有独立的日志文件
- 可以独立启动/停止/重启每个服务

**优点**：
- ✅ 真正的隔离，互不影响
- ✅ 可以连接不同的 FRP 服务器
- ✅ 某个服务失败不影响其他服务
- ✅ 可以独立查看每个服务的日志

**缺点**：
- ❌ 需要修改较多代码
- ❌ 占用更多系统资源（多个 frpc 进程）
- ❌ 需要管理多个配置文件

**目录结构**：
```
%APPDATA%/frpc-gui/
├── settings.json
├── frpc-gui-data.json
└── servers/
    ├── server-1/
    │   ├── config.toml
    │   └── frpc.log
    ├── server-2/
    │   ├── config.toml
    │   └── frpc.log
    └── server-3/
        ├── config.toml
        └── frpc.log
```

---

### 方案 B：单配置文件 + 单进程（当前方案）

**架构**：
```
AppState
├── process_manager: Mutex<Option<FrpProcessManager>>  // 一个进程
└── config_manager: Mutex<Option<ConfigManager>>       // 一个配置
```

**实现**：
- 所有代理共享一个 FRP 进程
- 所有代理在同一个配置文件中
- 通过 `server_id` 字段在 UI 层面关联
- **但实际上所有代理都连接到同一个 FRP 服务器**

**问题**：
- ❌ 代理的 `server_id` 字段只是 UI 标记
- ❌ 实际运行时，所有代理都在同一个 frpc 进程中
- ❌ 无法连接到不同的 FRP 服务器
- ❌ 配置文件只能有一个 `serverAddr`

**当前实现的问题**：
```toml
# frpc.toml 只能有一个服务器配置
serverAddr = "192.168.1.100"
serverPort = 7000

[[proxies]]
name = "proxy1"
# ← 无法指定不同的服务器
```

---

## 推荐实现：方案 A（多进程支持）

### 数据结构修改

#### 后端 (`src-tauri/src/commands/mod.rs`)

```rust
pub struct AppState {
    // 多个进程管理器，key 为服务器 ID
    pub process_managers: Mutex<HashMap<String, FrpProcessManager>>,
    // 多个配置管理器
    pub config_managers: Mutex<HashMap<String, ConfigManager>>,
    pub log_tx: Mutex<Option<mpsc::Sender<String>>>,
    pub settings_manager: Mutex<Option<SettingsManager>>,
    pub version_manager: Mutex<Option<FrpVersionManager>>,
}
```

#### 服务器数据结构

```typescript
interface Server {
  id: string;
  name: string;
  serverAddr: string;
  serverPort: number;
  token: string;
  tlsEnable: boolean;
  enabled: boolean;
  active: boolean;      // 是否正在运行
  pid?: number;         // 进程 ID（如果运行中）
  status?: 'running' | 'stopped' | 'error';
  errorMessage?: string;
}
```

### API 设计

#### 新增命令

```rust
// 启动指定服务器的 FRP 进程
#[tauri::command]
pub async fn start_server(server_id: String, state: State<'_, AppState>) -> Result<bool, String>

// 停止指定服务器的 FRP 进程
#[tauri::command]
pub async fn stop_server(server_id: String, state: State<'_, AppState>) -> Result<bool, String>

// 重启指定服务器的 FRP 进程
#[tauri::command]
pub async fn restart_server(server_id: String, state: State<'_, AppState>) -> Result<bool, String>

// 获取所有服务器状态
#[tauri::command]
pub async fn get_all_servers_status(state: State<'_, AppState>) -> Result<Vec<ServerStatus>, String>

// 获取单个服务器状态
#[tauri::command]
pub async fn get_server_status(server_id: String, state: State<'_, AppState>) -> Result<ServerStatus, String>
```

### 前端修改

#### 服务器卡片新增操作

```vue
<template #actions>
  <a @click="startServer(server)" v-if="!server.active">启动</a>
  <a @click="stopServer(server)" v-else>停止</a>
  <a @click="restartServer(server)">重启</a>
  <a @click="openEdit(server)">编辑</a>
  <a @click="deleteServer(server)" class="text-danger">删除</a>
</template>
```

#### 状态显示

```vue
<div class="server-status">
  <a-tag :color="statusColor(server.status)">
    {{ server.status === 'running' ? '运行中' : '已停止' }}
  </a-tag>
  <span v-if="server.pid" class="pid-badge">PID: {{ server.pid }}</span>
</div>
```

### 代理关联逻辑

```typescript
// 代理配置中的 server_id 决定它属于哪个 FRP 进程
// 启动服务器时，只启动关联到该服务器的代理

async function startServer(server: any) {
  // 获取该服务器的所有代理
  const serverProxies = appStore.proxies.filter(p => p.server_id === server.id);
  
  // 构建只包含这些代理的配置
  const config: FrpConfig = {
    serverAddr: server.serverAddr,
    serverPort: server.serverPort,
    token: server.token,
    tlsEnable: server.tlsEnable,
    proxies: serverProxies,
    // ...
  };
  
  // 启动该服务器的 FRP 进程
  await invoke('start_server', { serverId: server.id, config });
}
```

---

## 实现工作量估算

| 模块 | 工作量 | 说明 |
|------|--------|------|
| 后端状态管理 | 4 小时 | 改为 HashMap 管理多个实例 |
| 进程管理 | 4 小时 | 支持多进程启动/停止 |
| 配置管理 | 3 小时 | 每个服务器独立配置文件 |
| API 命令 | 3 小时 | 新增多服务器相关命令 |
| 前端 Store | 2 小时 | 支持多服务器状态 |
| 服务器视图 | 4 小时 | 添加启动/停止/状态显示 |
| 代理关联 | 2 小时 | 确保代理正确关联到服务器 |
| 测试调试 | 4 小时 | 全面测试多服务器场景 |
| **总计** | **26 小时** | 约 3-4 个工作日 |

---

## 简化方案：单服务器多代理（当前）

如果不需要同时连接多个 FRP 服务器，可以保持当前架构：

**特点**：
- 只能运行一个 FRP 进程
- 所有代理共享同一个服务器配置
- `server_id` 仅用于 UI 分组和标记
- 适合只需要连接一个 FRP 服务器的场景

**问题**：
- 代理的 `server_id` 字段没有实际作用
- 无法发挥多服务器的优势

**建议**：
- 如果只是 UI 分组，可以去掉 `server_id` 字段
- 或者明确说明当前只支持单服务器运行

---

## 建议

### 短期（MVP）
保持当前单进程架构，但明确说明：
> 当前版本只支持同时运行一个 FRP 服务。代理的"所属服务器"仅用于 UI 分组，不会实际连接到不同服务器。

### 中期
实现方案 A（多进程支持）：
- 支持同时运行多个 FRP 服务
- 每个服务连接不同的服务器
- 独立管理每个服务的生命周期

### 长期
考虑更高级的功能：
- 服务组管理
- 负载均衡
- 故障转移
- 统一监控面板

---

**创建时间**: 2026-08-25  
**状态**: 分析完成，等待决策
