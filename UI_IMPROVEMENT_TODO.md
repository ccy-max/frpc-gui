# UI 改进和服务器关联

## 任务
1. ✅ 代理列表改成卡片形式（服务器列表已经是卡片了）
2. ✅ 代理配置中添加服务器关联字段

## 问题
- 代理配置中没有 `server_id` 字段，无法知道代理属于哪个服务器
- 代理列表使用表格展示，需要改成卡片

## 解决方案

### 1. 数据结构修改
在代理对象中添加 `server_id` 字段，关联到服务器

### 2. UI 修改
- 代理列表从表格改成卡片网格
- 代理表单中添加服务器选择下拉框

### 3. 配置文件结构
frpc.toml 中的代理是属于 server 配置的一部分，需要正确映射
