# FRPC GUI 后续优化计划

**更新时间**: 2026-08-25  
**当前版本**: v0.1.0 (单进程模式)

---

## 📋 目录

1. [短期优化（1-2 周）](#短期优化)
2. [中期优化（1-2 月）](#中期优化)
3. [长期优化（3-6 月）](#长期优化)
4. [功能优先级投票](#优先级投票)

---

## 🎯 短期优化（1-2 周）

### 1. 用户体验改进 ⭐⭐⭐⭐⭐

#### 1.1 删除服务器时的关联检查
**问题**: 删除服务器后，关联的代理显示"未关联"，体验不佳

**方案**:
```typescript
async function deleteServer(id: string) {
  // 检查是否有关联的代理
  const relatedProxies = proxies.value.filter(p => p.server_id === id);
  
  if (relatedProxies.length > 0) {
    // 弹出确认对话框
    Modal.confirm({
      title: '确认删除',
      content: `该服务器下有关联的 ${relatedProxies.length} 个代理，删除后这些代理将失去关联。`,
      okText: '继续删除',
      cancelText: '取消',
      onOk: () => {
        // 可选：批量清空代理的 server_id
        relatedProxies.forEach(p => updateProxy(p.name, { server_id: null }));
        deleteServerFromList(id);
      }
    });
  } else {
    deleteServerFromList(id);
  }
}
```

**工作量**: 2 小时  
**优先级**: ⭐⭐⭐⭐⭐

---

#### 1.2 批量修改代理的服务器关联
**问题**: 多个代理需要修改关联服务器时，需要逐个编辑

**方案**:
- 代理列表添加复选框
- 批量操作工具栏
- 批量修改所属服务器

**UI**:
```
┌─────────────────────────────────────────┐
│ ☑ web-proxy      [TCP]  生产服务器     │
│ ☑ api-proxy      [HTTP] 生产服务器     │
│ ☐ db-proxy       [TCP]  测试服务器     │
│ ☑ cache-proxy    [TCP]  生产服务器     │
└─────────────────────────────────────────┘
[已选择 3 项] [批量修改服务器 ▼] [批量删除]
```

**工作量**: 4 小时  
**优先级**: ⭐⭐⭐⭐

---

#### 1.3 服务器状态显示关联的代理数量
**问题**: 无法快速知道每个服务器有多少代理

**方案**:
```vue
<div class="server-card">
  <div class="server-info-row">
    <span class="info-label">代理：</span>
    <a-tag color="blue">
      {{ getProxyCount(server.id) }} 个
    </a-tag>
  </div>
</div>
```

**工作量**: 1 小时  
**优先级**: ⭐⭐⭐⭐⭐

---

#### 1.4 默认服务器被删除时的处理
**问题**: 默认服务器被删除后，设置中没有清理

**方案**:
```typescript
async function deleteServer(id: string) {
  if (appStore.defaultServerId === id) {
    appStore.setDefaultServerId(null);
    message.warning('默认服务器已删除，默认设置已清空');
  }
  // ... 删除逻辑
}
```

**工作量**: 0.5 小时  
**优先级**: ⭐⭐⭐⭐

---

### 2. 数据验证和错误处理 ⭐⭐⭐⭐

#### 2.1 代理配置验证
**验证项**:
- [ ] 名称不能重复
- [ ] 端口范围检查 (1-65535)
- [ ] IP 地址格式验证
- [ ] 远程端口不能与本地端口冲突
- [ ] HTTP 代理的域名格式

**工作量**: 3 小时  
**优先级**: ⭐⭐⭐⭐

---

#### 2.2 服务器配置验证
**验证项**:
- [ ] 地址不能为空
- [ ] 端口范围检查
- [ ] 连接测试功能
- [ ] Token 强度提示

**工作量**: 3 小时  
**优先级**: ⭐⭐⭐⭐

---

### 3. UI/UX 细节优化 ⭐⭐⭐

#### 3.1 空状态优化
**当前**: 简单的 empty description

**优化**:
```vue
<a-empty>
  <template #description>
    <div class="empty-guide">
      <p>暂无代理配置</p>
      <a-button type="primary" @click="openAdd">
        <PlusOutlined /> 添加第一个代理
      </a-button>
      <p class="help-text">
        💡 提示：先添加服务器，再添加代理
      </p>
    </div>
  </template>
</a-empty>
```

**工作量**: 2 小时  
**优先级**: ⭐⭐⭐

---

#### 3.2 快捷操作提示
**功能**: 鼠标悬停显示快捷键提示

**工作量**: 1 小时  
**优先级**: ⭐⭐

---

#### 3.3 代理卡片增强
**新增信息**:
- 带宽限制（如果配置了）
- 流量限制（如果配置了）
- 加密和压缩状态图标

**工作量**: 2 小时  
**优先级**: ⭐⭐⭐

---

## 🚀 中期优化（1-2 月）

### 4. 多 FRP 进程支持 ⭐⭐⭐⭐⭐

**当前架构**: 单进程，所有代理共享一个配置

**目标架构**: 每个服务器独立进程

#### 4.1 后端架构改造
**修改内容**:
```rust
pub struct AppState {
    // 当前
    pub process_manager: Mutex<Option<FrpProcessManager>>,
    
    // 改造后
    pub process_managers: Mutex<HashMap<String, FrpProcessManager>>,
    pub config_managers: Mutex<HashMap<String, ConfigManager>>,
}
```

**新增 API**:
- `start_server(server_id)` - 启动指定服务器
- `stop_server(server_id)` - 停止指定服务器
- `restart_server(server_id)` - 重启指定服务器
- `get_all_servers_status()` - 获取所有服务器状态

**工作量**: 26 小时（约 3-4 工作日）  
**优先级**: ⭐⭐⭐⭐⭐  
**风险**: 高（架构改动大）

---

#### 4.2 前端多服务器支持
**修改内容**:
- 服务器卡片增加启动/停止/重启按钮
- 显示每个服务器的运行状态（PID、运行时长）
- 独立的日志查看
- 按服务器分组显示代理

**工作量**: 16 小时（约 2 工作日）  
**优先级**: ⭐⭐⭐⭐⭐

---

#### 4.3 配置文件管理
**目录结构**:
```
%APPDATA%/frpc-gui/
├── settings.json
├── frpc-gui-data.json
└── servers/
    ├── server-1/
    │   ├── config.toml
    │   └── frpc.log
    ├── server-2/
    │   ├── config.toml
    │   └── frpc.log
    └── server-3/
        ├── config.toml
        └── frpc.log
```

**工作量**: 8 小时  
**优先级**: ⭐⭐⭐⭐⭐

---

### 5. 高级功能 ⭐⭐⭐⭐

#### 5.1 代理模板
**功能**: 保存常用的代理配置为模板

**使用场景**:
- 快速创建相同类型的代理
- 标准化配置
- 团队共享

**UI**:
```
┌─────────────────────────────────────┐
│ 代理模板                     [×]    │
├─────────────────────────────────────┤
│ + 新建模板                           │
│                                      │
│ ┌─────────────────────────────────┐ │
│ │ 📦 Web 服务模板                  │ │
│ │ 类型：HTTP                       │ │
│ │ 本地端口：8080                   │ │
│ │ [应用] [编辑] [删除]             │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**工作量**: 6 小时  
**优先级**: ⭐⭐⭐⭐

---

#### 5.2 配置导入/导出增强
**当前**: Base64 字符串

**增强**:
- JSON 文件导出/导入
- 批量导出多个代理
- 从 URL 导入配置
- 配置版本历史

**工作量**: 8 小时  
**优先级**: ⭐⭐⭐

---

#### 5.3 端口扫描和冲突检测
**功能**:
- 启动前自动检测端口占用
- 建议可用端口
- 一键修改冲突端口

**工作量**: 6 小时  
**优先级**: ⭐⭐⭐⭐

---

### 6. 监控和告警 ⭐⭐⭐⭐

#### 6.1 实时流量监控
**功能**:
- 每个代理的实时流量
- 历史流量图表
- 流量统计（日/周/月）

**UI**:
```
┌─────────────────────────────────────┐
│ 📊 web-proxy 流量监控               │
├─────────────────────────────────────┤
│ ↑ 上传：1.2 GB  今日                 │
│ ↓ 下载：5.8 GB  今日                 │
│                                      │
│ [图表：24 小时流量趋势]              │
└─────────────────────────────────────┘
```

**工作量**: 12 小时  
**优先级**: ⭐⭐⭐⭐

---

#### 6.2 连接状态监控
**功能**:
- 实时检测代理连接状态
- 断线通知（系统通知/声音）
- 自动重连统计

**工作量**: 10 小时  
**优先级**: ⭐⭐⭐⭐⭐

---

#### 6.3 告警规则
**功能**:
- 流量超限告警
- 连接失败告警
- 自定义告警规则

**工作量**: 8 小时  
**优先级**: ⭐⭐⭐

---

## 🔮 长期优化（3-6 月）

### 7. 云同步和协作 ⭐⭐⭐

#### 7.1 配置云同步
**功能**:
- 配置同步到云端
- 多设备同步
- 配置备份和恢复

**工作量**: 20 小时  
**优先级**: ⭐⭐⭐

---

#### 7.2 团队配置共享
**功能**:
- 配置分享给团队成员
- 权限管理（只读/编辑）
- 配置变更历史

**工作量**: 24 小时  
**优先级**: ⭐⭐

---

### 8. 插件系统 ⭐⭐⭐

#### 8.1 插件 API
**功能**:
- 开放插件 API
- 支持第三方插件
- 插件市场

**工作量**: 40 小时  
**优先级**: ⭐⭐⭐

---

#### 8.2 内置插件
**插件创意**:
- Web 管理界面
- 自动化脚本
- 自定义协议支持
- 第三方服务集成

**工作量**: 每个插件 8-16 小时  
**优先级**: ⭐⭐⭐

---

### 9. 性能优化 ⭐⭐⭐⭐

#### 9.1 大量代理优化
**目标**: 支持 1000+ 代理
**方案**:
- 虚拟滚动
- 分页加载
- 懒加载

**工作量**: 12 小时  
**优先级**: ⭐⭐⭐⭐

---

#### 9.2 启动速度优化
**目标**: 冷启动 < 2 秒
**方案**:
- 代码分割
- 预加载关键资源
- 延迟加载非关键组件

**工作量**: 16 小时  
**优先级**: ⭐⭐⭐⭐

---

### 10. 国际化 ⭐⭐⭐

#### 10.1 多语言支持
**语言**:
- 简体中文 (已有)
- 英文
- 繁体中文
- 日语
- 韩语

**工作量**: 每个语言 8 小时  
**优先级**: ⭐⭐⭐

---

## 📊 优先级投票

### 必做功能（P0）
- [ ] 多 FRP 进程支持
- [ ] 删除服务器时的关联检查
- [ ] 服务器状态显示代理数量
- [ ] 连接状态监控

### 重要功能（P1）
- [ ] 批量修改代理的服务器关联
- [ ] 代理配置验证
- [ ] 端口扫描和冲突检测
- [ ] 实时流量监控
- [ ] 代理模板

### 可选功能（P2）
- [ ] 空状态优化
- [ ] 配置导入/导出增强
- [ ] 告警规则
- [ ] 云同步

### 未来功能（P3）
- [ ] 团队配置共享
- [ ] 插件系统
- [ ] 国际化

---

## 🗓️ 开发路线图

### v0.2.0 (2 周后)
- ✅ 删除服务器关联检查
- ✅ 服务器显示代理数量
- ✅ 代理配置验证
- ✅ UI 细节优化

### v0.3.0 (1 月后)
- ✅ 批量操作
- ✅ 端口冲突检测
- ✅ 代理模板
- ✅ 配置增强

### v0.4.0 (2 月后)
- ✅ 多 FRP 进程支持
- ✅ 连接状态监控
- ✅ 流量监控

### v1.0.0 (6 月后)
- ✅ 云同步
- ✅ 插件系统
- ✅ 国际化
- ✅ 性能优化

---

## 💡 贡献指南

如果你想参与开发：
1. 选择感兴趣的功能
2. 创建 Issue 讨论实现方案
3. 开发并提交 PR
4. 代码审查和合并

---

## 📝 反馈渠道

- GitHub Issues: 功能请求和 Bug 报告
- 讨论区：想法和建议
- 邮件：商业合作

---

**最后更新**: 2026-08-25  
**维护者**: FRPC GUI Team
