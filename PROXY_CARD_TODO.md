# 代理卡片化和服务关联 - 已完成 ✅

## 任务
- [x] 服务器列表已是卡片形式
- [x] 代理列表改为卡片形式
- [x] 添加代理与服务器的关联功能

## 实现方案

### 1. 代理数据结构修改 ✅
在代理对象中添加 `server_id` 字段，用于关联服务器

### 2. 代理表单修改 ✅
添加"所属服务器"下拉选择框
- 默认选中活动服务器
- 可以选择任意已配置的服务器
- 可以不关联服务器

### 3. 代理卡片视图 ✅
参考服务器卡片，设计代理卡片展示：
- ✅ 代理名称和类型（带图标）
- ✅ 关联的服务器（带标签）
- ✅ 本地地址和远程端口
- ✅ 状态（运行中/已停止）
- ✅ 操作按钮（启动/停止/编辑/删除）
- ✅ 带宽限制和流量限制（如果有）

### 4. 过滤功能 ✅
- ✅ 按服务器过滤代理列表
- ✅ 关键词搜索（名称或类型）
- ✅ 两种过滤可以组合使用

## 新增功能

### 卡片特性
- **视觉区分**：运行中的代理有绿色边框
- **类型标签**：不同代理类型有不同颜色标签
- **图标标识**：HTTP/HTTPS 用地球图标，STCP/XTCP 用闪电图标，其他用应用图标
- **悬停效果**：卡片悬停时上移并显示阴影

### 服务器关联
- **添加代理时**：默认选中活动服务器
- **编辑代理时**：可以修改关联的服务器
- **卡片展示**：显示关联的服务器名称标签
- **过滤功能**：可以按服务器筛选代理

## 修改的文件
- `src/views/Proxies.vue` - 完全重写为卡片布局

## 下一步
- [ ] 编译测试
- [ ] 验证功能
