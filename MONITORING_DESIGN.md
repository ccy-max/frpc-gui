# 连接状态监控和流量监控 - 设计方案

**开始时间**: 2026-08-25 15:39  
**预计完成**: 2026-08-25 18:00

---

## 🎯 目标

1. **连接状态监控**: 实时检测每个代理的连接状态
2. **流量监控**: 显示每个代理的实时流量和历史统计

---

## 📐 架构设计

### 连接状态监控

**后端**:
- 定期检测每个代理的连接状态
- 通过 frpc 的 admin API 获取状态
- 断线时发送通知

**前端**:
- 显示代理连接状态（在线/离线）
- 断线时显示红色警告
- 系统通知（可选）

### 流量监控

**后端**:
- 通过 frpc 的 admin API 获取流量数据
- 记录历史流量（每小时/每天）
- 提供流量统计 API

**前端**:
- 显示实时流量（上传/下载）
- 显示今日流量统计
- 流量趋势图表

---

## 🔧 实现方案

### 方案 A: 通过 frpc admin API（推荐）

frpc 提供了 admin HTTP API，可以查询：
- 代理状态
- 流量统计
- 连接信息

**配置**:
```toml
[admin]
addr = "127.0.0.1"
port = 7400
user = "admin"
password = "admin"
```

**API 端点**:
- `GET /api/status` - 获取所有代理状态
- `GET /api/proxies/:name` - 获取指定代理详情
- `GET /api/proxies/:name/traffic` - 获取代理流量

**优点**:
- ✅ frpc 原生支持
- ✅ 数据准确
- ✅ 无需修改 frpc

**缺点**:
- ✅ 需要启动 admin HTTP 服务
- ✅ 需要轮询（实时性有限）

---

### 方案 B: 解析 frpc 日志

通过分析 frpc.log 日志文件：
- 提取连接/断线事件
- 提取流量统计

**优点**:
- ✅ 无需额外配置
- ✅ 历史记录完整

**缺点**:
- ❌ 解析复杂
- ❌ 实时性差
- ❌ 日志格式可能变化

---

## 📋 开发步骤

### Step 1: 后端 - Admin API 客户端 (3 小时)
- [ ] 创建 HTTP 客户端连接 frpc admin
- [ ] 实现状态查询函数
- [ ] 实现流量查询函数
- [ ] 添加错误处理

### Step 2: 后端 - 状态轮询服务 (2 小时)
- [ ] 定期轮询每个服务器的状态
- [ ] 检测连接变化
- [ ] 发送通知（可选）

### Step 3: 后端 - 新增 API 命令 (2 小时)
- [ ] `get_proxy_status` - 获取代理状态
- [ ] `get_proxy_traffic` - 获取代理流量
- [ ] `get_server_traffic` - 获取服务器总流量

### Step 4: 前端 - 状态显示 (2 小时)
- [ ] 代理卡片显示连接状态
- [ ] 断线时红色警告
- [ ] 状态图标和提示

### Step 5: 前端 - 流量显示 (3 小时)
- [ ] 代理卡片显示实时流量
- [ ] 流量详情弹窗
- [ ] 简单的流量图表

---

## 🎯 成功标准

- [ ] 能查询每个代理的连接状态
- [ ] 断线时前端显示警告
- [ ] 显示实时流量（今日上传/下载）
- [ ] 流量数据每 30 秒更新
- [ ] 无性能问题

---

**状态**: 设计完成，准备开发
